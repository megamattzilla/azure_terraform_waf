{
        "class": "ADC",
        "schemaVersion": "3.18.0",
        "id": "placeholder",
        "label": "Ansibe Declaration",
        "remark": "Generated by Ansible",
        "Common": {
            "class": "Tenant",
            "Shared": {
                "class": "Application",
                "template": "shared",
{% for service in Service_HTTP %}
              {{ service | to_nice_json(indent=2) | regex_replace('^\\{|\\}$', '') | trim('\n') | indent(12) }},
{% endfor %}
{% for service in Service_HTTPS %}
              {{ service | to_nice_json(indent=2) | regex_replace('^\\{|\\}$', '') | trim('\n') | indent(12) }},
{% endfor %}
{% for pool in Pool %}
              {{ pool | to_nice_json(indent=2) | regex_replace('^\\{|\\}$', '') | trim('\n') | indent(12) }},
{% endfor %}
{% for monitor in Monitor %}
              {{ monitor | to_nice_json(indent=2) | regex_replace('^\\{|\\}$', '') | trim('\n') | indent(12) }},
{% endfor %}
{% for profile in Security_Log_Profile %}
              {{ profile | to_nice_json(indent=2) | regex_replace('^\\{|\\}$', '') | trim('\n') | indent(12) }},
{% endfor %}
{% for profile in TLS_Client %}
              {{ profile | to_nice_json(indent=2) | regex_replace('^\\{|\\}$', '') | trim('\n') | indent(12) }},
{% endfor %}
{% for profile in TLS_Server %}
              {{ profile | to_nice_json(indent=2) | regex_replace('^\\{|\\}$', '') | trim('\n') | indent(12) }},
{% endfor %}
{% for certificate in Certificate %}
              {{ certificate | to_nice_json(indent=2) | regex_replace('^\\{|\\}$', '') | trim('\n') | indent(12) }},
{% endfor %}
{% for profile in TCP_Profile %}
              {{ profile | to_nice_json(indent=2) | regex_replace('^\\{|\\}$', '') | trim('\n') | indent(12) }},
{% endfor %}
{% for profile in HTTP_Profile %}
              {{ profile | to_nice_json(indent=2) | regex_replace('^\\{|\\}$', '') | trim('\n') | indent(12) }}{{ "," if not loop.last }}
{% endfor %}
        }
    }
}
